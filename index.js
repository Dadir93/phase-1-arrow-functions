function multiply(a, b) {
    return a * b;
  }
  
  function divide() {
    return 2000 / 100;
  }
  
  console.log(divide());
  
  const square = (x) => x * x;
  const add = (a, b) => a + b;
  
  module.exports = { multiply, divide, square, add };
  